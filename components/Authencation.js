import React, { Component } from 'react';
import { Text, View } from 'react-native';

export default class Authencation extends Component {
  render() {
    return (
      <View>
            <Text style={{ color: '#00ff00' }}> Authencation component </Text>
      </View>
    );
  }
}
